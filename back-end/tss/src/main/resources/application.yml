server:
  port: 8085
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tss_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
        dialect: org.hibernate.dialect.MySQL5Dialect
    platform: mysql

logging:
  level:
    org:
      springframework:
        security: DEBUG

